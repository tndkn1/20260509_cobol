# COBOLで日本語の文字列（固定長）を扱うベストプラクティス

COBOLで固定長のファイルを扱う場合、日本語（全角文字）をどのように処理するかは、対象の環境や文字コードによって大きく2つのアプローチに分かれます。

## 1. 従来型（Shift_JIS / CP932 / EBCDIC等）

メインフレームや従来のオフコン、Windows環境でのCOBOL開発で最も一般的なアプローチです。

### 特徴
- 全角文字は**「常に2バイト」**となります。
- `PIC X(2)` で全角1文字として計算しやすいため、COBOLの文字数・バイト数の計算が非常に直感的です。

### 実装例
全角30文字の氏名を定義する場合：
```cobol
05  IN-NAME      PIC X(60).  *> 全角30文字 × 2バイト = 60バイト
```

### メリット・デメリット
- **メリット**: バイト数と文字数が比例するため、位置の計算や切り出し（部分参照）が容易です。
- **デメリット**: 現代のWebシステムやLinux環境はUTF-8が主流のため、他システムと連携する際に文字コード変換が必要になります。

---

## 2. 現代型（UTF-8）

現在のLinux環境（GnuCOBOLなど）や最新のテキストエディタと最も相性が良いアプローチです。

### 特徴
- 日本語の全角文字は主に**「3バイト」**になります（一部の特殊文字は4バイト）。
- 固定長のバイト数を計算する際、「最大のバイト数」をあらかじめ割り当てておきます。

### 実装例
全角30文字の氏名を定義する場合：
```cobol
05  IN-NAME      PIC X(90).  *> 全角30文字 × 最大3バイト = 90バイト
```

### データ作成時の注意点（パディング）
UTF-8は可変長エンコーディングのため、データを作成する側（Pythonなどのプログラム）で、**「指定のバイト数に達するまで半角スペース等でパディング（空白埋め）する」**処理を正確に行う必要があります。

Pythonでのパディング例：
```python
def pad_utf8(text, length):
    # UTF-8でエンコード
    encoded = text.encode('utf-8')
    if len(encoded) > length:
        # 切り捨て処理（※マルチバイト文字の途中で切れないよう注意が必要）
        return encoded[:length]
    else:
        # 足りないバイト数分、半角スペース(0x20)でパディング
        return encoded + b' ' * (length - len(encoded))
```

### メリット・デメリット
- **メリット**: ソースコードもデータファイルもUTF-8で統一できるため、環境依存の文字化けリスクが減少し、エディタ等での閲覧・編集が容易です。
- **デメリット**: バイト数ベースでのパディング処理が必要となり、文字列長とバイト数が一致しないため、COBOL内での文字単位の部分参照が難しくなります。

---

## 推奨されるベストプラクティス
**「既存のメインフレーム資産を移行するか」「新規のLinux/GnuCOBOL環境で開発するか」**で方針を決定します。

- 既存のレガシーシステムとの互換性が重要であれば、**「Shift_JISベースで扱う（全角1文字＝2バイト）」**
- 独立したモダンなLinux環境での開発・連携を前提とするなら、**「UTF-8で統一し、バイト数ベースでパディングした固定長ファイルとする（全角1文字＝3バイト想定）」**

状況に合わせてこれらを選択することが、文字化けやフォーマット崩れを防ぐ最良の方法です。
